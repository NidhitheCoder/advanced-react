import type { ComponentStyleConfig } from "@chakra-ui/react";

const Button: ComponentStyleConfig = {
  baseStyle: {
    fontsize: "16px",
    fontWeight: "500",
    fontcolor: "black",
    backgroundColor: 'cucumbar',
  },
  sizes: {},
  variants: {},
  defaultProps: {},
};

export default Button;
